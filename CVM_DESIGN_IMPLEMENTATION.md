# 지도·시각 인지 모듈(CVM) 설계 및 구현

본 절에서는 프로젝트 전체 구조 중 지도·시각 인지 모듈(CVM) 설계 및 구현 방법을 중심으로 세부 개발 내용을 기술한다. 시스템 전체 흐름은 자연어 지시와 카메라 이미지를 입력으로 받아, CSM(지시·제약 관리), CVM(시각 인지 및 value map 계산), NAV(다음 이동 방향 결정), AR 인터페이스(안내 화면 출력) 순으로 동작하도록 설계하였다. 이 중 CVM과 NAV 브릿지 부분은 실내 공간을 그래프 기반 미니맵과 시각 정보로 표현하고, 현재 위치와 다음 이동 방향을 정량적으로 계산하는 핵심 역할을 담당한다.

## 1. 그래프 기반 미니맵 구축

AI공학관 4층에 대해서는 먼저 실제 건물 구조를 조사하여 그래프 기반 미니맵을 구축하였다. 4층 복도, 교차로, 엘리베이터, 계단, 화장실, 강의실 앞 지점 등을 노드로 정의하고, 각 노드에 대해 고유 ID, 용도(type), 위치 설명(description), 방 번호 범위(room_range) 및 기준 방(anchor_room) 정보를 부여하였다. 노드 간 연결 관계는 `neighbors` 컬럼에 세미콜론으로 인접 노드를 기록하는 방식으로 표현하여, CSV만으로도 NetworkX 그래프를 복원할 수 있도록 하였다.

이후 4층 각 지점에서 촬영한 대표 이미지를 이용해 CLIP 기반 이미지 임베딩을 생성하고, 이를 `clip_embedding` 컬럼에 저장함으로써 하나의 CSV 파일 안에 그래프 구조와 시각 정보를 동시에 포함시키도록 설계하였다. 이렇게 구축된 미니맵 데이터는 `ai_4f_node_map_fixed_embeded.csv` 파일에 저장되며, 각 행은 하나의 노드를 나타내고, `node_id`, `floor`, `description`, `neighbors`, `type`, `room_range`, `anchor_room`, `clip_embedding` 등의 컬럼을 포함한다.

미니맵 로딩 과정에서는 `map_loader.py` 모듈이 CSV 파일을 읽어 NetworkX 그래프 객체와 노드 메타데이터 딕셔너리, 임베딩 행렬을 생성한다. 임베딩 벡터는 L2 정규화를 거쳐 코사인 유사도 계산에 적합한 형태로 변환되며, 노드 ID와 임베딩 행렬의 인덱스 간 매핑 정보도 함께 반환된다.

## 2. 현재 위치 추정 (Localization)

CVM의 첫 단계는 카메라로 촬영한 현재 시야 이미지가 미니맵 상의 어느 노드에 해당하는지 추정하는 과정이다. 이를 위해 OpenCLIP 모델을 사용하여 입력 이미지를 임베딩 벡터로 변환하고, 앞서 구축한 노드 임베딩 행렬과의 코사인 유사도를 계산하여 상위 후보 노드를 선정하였다.

단순 유사도만으로는 유사한 복도 구조 사이에서 혼동이 발생할 수 있으므로, 이미지 내 방 번호 텍스트를 OCR로 추출하고 각 노드의 `room_range`·`anchor_room` 정보와 비교하여 일치하거나 근접하는 노드에 보정 점수를 부여하는 방식을 추가하였다. OCR 처리 과정에서는 EasyOCR을 사용하며, 형태학적 기법을 통한 ROI(Region of Interest) 검출, CLAHE 대비 향상, 언샤프 마스킹, 적응형 임계값 등의 전처리 기법을 적용하여 텍스트 인식 정확도를 향상시켰다. 또한 TTA(Test-Time Augmentation)를 통해 회전 및 스케일 변환된 이미지에 대해서도 OCR을 수행하여 인식률을 개선하였다.

일부 상황에서는 후보 노드의 레퍼런스 이미지와 입력 이미지를 SIFT로 기하학적으로 비교하여, 실제 동일 지점일 가능성을 추가 점수로 반영할 수 있도록 구성하였다. SIFT 특징점 검출 후 BFMatcher를 통한 특징점 매칭, RANSAC 호모그래피 추정을 거쳐 인라이어 비율을 계산하고, 이를 기하학적 검증 점수로 활용한다.

또한 이전 노드 정보를 활용한 그래프 사전 확률과, 이웃 노드들 간의 일관성을 고려한 보너스 점수도 추가하여 시간적 일관성을 확보하였다. 최종적으로 CLIP 유사도, 방 번호 OCR 결과, 기하 검증 결과, 그래프 사전 확률, 이웃 일관성 점수를 가중합하여 가장 점수가 높은 노드를 현재 위치(`current_node`)로 결정하고, 상위 후보 목록과 함께 `CvmResult` 형태로 반환하도록 구현하였다.

가중치 조정은 실험을 통해 최적화되었으며, 기본값으로는 CLIP 가중치 1.0, OCR 가중치 0.3, 기하학적 검증 가중치 0.4, 그래프 사전 확률 가중치 0.2, OCR-노드ID 직접 매칭 가중치 0.6, 이웃 일관성 가중치 0.4를 사용한다. 모든 보조 점수는 0~1 범위로 정규화된 후 가중합되며, 최종 confidence는 온도 스케일링을 적용한 softmax 확률로 계산된다.

## 3. Value Map 계산

현재 위치가 결정되면, 다음 단계에서는 여러 이웃 노드 중 어느 방향으로 이동하는 것이 좋은지 결정하기 위해 value map을 계산한다. Value map은 현재 노드의 각 이웃 노드에 대해 목표까지의 가치를 정량화한 것으로, 값이 클수록 해당 방향으로 이동하는 것이 유리함을 의미한다.

### 3.1 Value Map v1 (기본 버전)

초기 설계에서는 CSM에서 제공하는 `route_nodes` 및 `target_nodes` 정보를 이용해, 그래프 상에서 경로를 따라 앞으로 나아가는 방향과 이미 지나온 노드, 경로에서 벗어나는 방향을 구분하는 기본 value map을 구성하였다. 현재 노드의 이웃 노드 각각에 대해 경로 상 위치와 거리, 방문 여부를 기반으로 점수를 계산하고, 이를 0~1 범위로 정규화하여 이웃 노드별 가치로 사용하는 방식이다.

구체적으로, 각 이웃 노드에 대해 다음 규칙을 적용한다:
- 경로 상 앞 방향에 가중치를 부여: 이웃 노드가 `route_nodes` 내에 있고, 현재 노드보다 목표에 가까운 위치에 있으면 높은 점수 부여
- 이미 지나온 노드에 페널티: `visited_nodes`에 포함된 이웃 노드에는 낮은 점수 부여
- 경로 밖 노드에 페널티: `route_nodes`에 포함되지 않은 이웃 노드에는 감점 적용

이렇게 계산된 점수들을 0~1 범위로 정규화하여 최종 value map을 생성한다.

### 3.2 Value Map v2 (확장 버전)

이후 테스트 과정에서 단순 그래프 정보만으로는 비전 기반 내비게이션의 장점을 충분히 살리지 못한다는 점을 확인하고, 시각 후보 정보와 이동 히스토리, 장애 요소 정보를 함께 고려하는 확장 버전(value map v2)을 설계하였다.

확장된 value map에서는 `CvmResult`에서 얻은 시각적 후보(`cvm_candidates`)를 이용해 현재 시야와 잘 맞는 방향에 추가 점수를 부여한다. 즉, 특정 이웃 노드가 CVM 위치 추정 과정에서 상위 후보로 선정되었다면, 해당 방향으로 이동했을 때 시각적으로 일관된 환경을 유지할 가능성이 높다는 점을 반영한다. 이를 CLIP-OCR 정합도 점수로 계산하여 value map에 반영한다.

과거 방문 이력을 기반으로 같은 구간을 반복해서 순환하는 패턴에는 감점 요소를 주도록 하였다. 이동 히스토리에서 최근 방문한 노드들을 분석하여, 특정 이웃 노드로 이동했을 때 순환 경로에 빠질 가능성이 높으면 해당 방향의 가치를 낮춘다.

또한 공사, 폐쇄 등 장애 요소 정보가 주어지는 경우 해당 방향의 가치를 크게 낮추어 실제 통행이 어려운 경로가 선택되지 않도록 하였다. 장애 요소는 `obstacles` 파라미터로 전달되며, 해당 노드로의 이동을 제한하거나 가치를 0에 가깝게 설정한다.

이렇게 계산된 route 기반 점수, 시각 정합도(CLIP-OCR), 히스토리 요소, 장애 요소를 가중합한 뒤 다시 0~1 범위로 정규화하여 최종 value map을 생성한다. 각 요소의 가중치는 실험을 통해 조정할 수 있으며, 기본적으로는 route 점수에 가장 높은 가중치를 부여하고, 시각 정합도와 히스토리 요소는 보조적으로 활용한다.

### 3.3 Value Map 비교

다음 표는 Value Map v1과 v2의 주요 차이점을 비교한 것이다.

| 구분 | Value Map v1 (기본 버전) | Value Map v2 (확장 버전) |
|------|-------------------------|-------------------------|
| **주요 입력** | `current_node`, `route_nodes`, `target_nodes`, `visited_nodes` | 모든 v1 입력 + `cvm_candidates` (시각 후보), `이동 히스토리`, `장애 요소(obstacles)` |
| **고려 정보** | 경로 상 위치, 앞으로 나아가는 방향 경로 여부, 방문 여부 | v1 정보 + 시야와의 정합도(CLIP-OCR), 반복 이동 패턴, 공사·폐쇄 등 장애 요소 |
| **계산 방식** | route 상 앞 방향에 가중치를 부여하고, 이미 지나온 노드와 경로 밖 노드에 페널티를 부여하여 0~1 범위로 정규화 | route 점수, 시각 정합도, 히스토리 요소, 장애 요소를 가중합한 뒤 0~1 범위로 정규화 |

## 4. 구현 세부사항

### 4.1 모듈 구조

CVM 모듈은 다음과 같은 파일들로 구성된다:

- `scripts/map_loader.py`: CSV 파일에서 그래프 및 임베딩 데이터 로드
- `scripts/localize_image.py`: 이미지 기반 위치 추정 및 다중 신호 융합
- `scripts/evaluate_cvm.py`: 대량 이미지에 대한 자동 평가 시스템

### 4.2 데이터 구조

**CvmResult 구조:**
```python
{
    "current_node": int,           # 현재 위치한 노드 ID
    "confidence": float,           # 위치 추정 신뢰도 (0~1)
    "candidates": [                # 상위 후보 노드 목록
        {"node_id": int, "score": float},
        ...
    ],
    "debug": {
        "ocr_numbers": List[str],  # OCR로 추출된 방 번호
        "weights": {...}           # 사용된 가중치 정보
    }
}
```

**ValueMap 구조:**
```python
{
    "current_node": int,
    "neighbor_values": {
        int: float   # {이웃 노드 id: value (클수록 좋은 방향)}
    }
}
```

### 4.3 성능 최적화

위치 추정 과정에서 다음과 같은 최적화 기법을 적용하였다:

1. **임베딩 차원 자동 매칭**: CSV의 임베딩 차원을 분석하여 적절한 CLIP 모델을 자동 선택하여 모델 불일치를 방지한다.

2. **후보 풀 확장**: 초기 CLIP Top-K 후보를 10배 확장(예: topk=5 → pool_size=50)하고, OCR 힌트로 누락된 노드를 강제 주입하여 재랭킹을 통해 정확도를 향상시킨다.

3. **정규화 및 스케일링**: 모든 보조 점수를 0~1 범위로 정규화하고, OCR이 없는 경우 OCR 관련 가중치를 0으로 설정하는 동적 가중치 조정을 수행한다.

4. **온도 스케일링**: Softmax에 온도 파라미터(τ=0.05)를 적용하여 confidence 계산의 안정성을 향상시킨다.

### 4.4 평가 시스템

`evaluate_cvm.py` 모듈을 통해 대량의 노드 이미지에 대해 자동으로 위치 추정 성능을 평가할 수 있다. 평가 지표로는 Top-1 정확도와 Top-K 정확도를 사용하며, 노드별 성능 분석과 실패 케이스 추적 기능도 제공한다. 이를 통해 다양한 파라미터 조합에 대한 성능을 체계적으로 측정하고 최적화할 수 있다.

## 5. 결론

본 절에서 기술한 CVM 모듈은 그래프 기반 미니맵과 시각 정보를 결합하여 실내 공간에서의 정확한 위치 추정과 이동 방향 결정을 가능하게 한다. CLIP 기반 유사도 계산, OCR 보정, 기하학적 검증, 그래프 사전 확률 등 다중 신호를 융합함으로써 단일 신호만으로는 해결하기 어려운 유사 환경 구분 문제를 해결하였으며, 확장된 value map을 통해 시각 정보와 이동 히스토리를 활용한 더욱 지능적인 경로 선택이 가능하도록 설계하였다.
